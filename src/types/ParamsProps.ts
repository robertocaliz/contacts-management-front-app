export type ParamsProps = {
    params: {
        id: string;
        email: string;
        activationToken: string;
        recoveryToken: string;
        contactId: string;
        userEmail: string;
    };
};
